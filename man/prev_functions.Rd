% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wip_prev_lexis.R
\name{prev_functions}
\alias{prev_functions}
\alias{prev_lexis}
\title{Prevalence}
\usage{
prev_lexis(
  lexis,
  observation_time_points,
  stratum_breaks,
  aggre_by = NULL,
  subset = NULL,
  first_record_by = NULL,
  merge_dt = NULL,
  merge_optional_args = NULL,
  n_at_risk_dt = NULL,
  weight_dt = NULL,
  conf_lvls = 0.95,
  conf_methods = "log"
)
}
\arguments{
\item{lexis}{\verb{[Lexis]} (no default)

A \code{Lexis} dataset (\verb{[Epi::Lexis]} / \verb{[Lexis_dt]}).}

\item{observation_time_points}{\verb{[list]} (no default)

Prevalence observation time points.
The \code{list} can have only one element, but multiple time points can be
supplied, e.g. \code{list(ts_cal = c(2009.999, 2010.999))}.
The output of this function will have these observation time points as
a column with the same name as the time scale.}

\item{stratum_breaks}{\verb{[list]} (no default)

Breaks to split \code{lexis} by. These are passed to \verb{[splitMulti]}. These
breaks create new strata in the output using the time scales in
\code{lexis}. The last time scale used here is assumed to be the follow-up time
time scale.
E.g. \code{list(ts_age = seq(0, 100, 10), ts_fut = c(0, 1, 5, Inf))}
to stratify output by age interval and time since entry interval at a
given observation point.}

\item{aggre_by}{Passed to \verb{[lexis_split_merge_aggregate_by_stratum]}.}

\item{subset}{\verb{[NULL, logical, integer, data.table]} (default \code{NULL})

This argument is evaluated within the context of \code{dt} and the environment
where the function with this argument was called.
\itemize{
\item \code{NULL}: Implies no subsetting, i.e. retain all observations.
\item \code{logical}: Keep only observations where this is \code{TRUE}. Must be of length
\code{nrow(dt)}. E.g. \code{subset = my_column == 1.}
\item \code{integer}: Keep only observations at these row numbers. E.g.
\code{subset = 1:5}.
\item \code{data.table}: Each column must also be a column of \code{dt}. Only those rows
in \code{dt} are retained that match a row in \code{subset}. E.g.
\code{subset = data.table::data.table(my_column = 1)}.
}

Due to the non-standard evaluation method used here, you can make use of
data in the calling environment as well. E.g. with variable \code{my_value = 1}
defined and \code{dt} containing column \code{my_column} you can do
\code{subset = my_column == my_value} if you want.}

\item{first_record_by}{Passed to \verb{[lexis_split_merge_aggregate_by_stratum]}.}

\item{merge_dt}{\verb{[NULL, data.table]} (default \code{NULL})

Table containing survival estimates applicable to \code{lexis}.
These survival
estimates are used in the "projection" of prevalence in those
observations
which have been lost to follow-up. See \strong{Functions} for how this
works and
what you need to have.
\itemize{
\item \code{NULL}: No projection is performed.
\item \code{data.table}: Must contain stratum columns also found in \code{lexis} and
exactly one value column named \code{S}. This table is passed
to \verb{[lexis_merge]} and should conform to its requirements. E.g.
\code{data.table(ts_fut = factor(c("[0, 1[", ...)), S = c(0.9, ...))}.
\item \code{list}: We produce a table of survival estimates on the fly and
these are arguments passed to \verb{[surv_lexis]}. See \strong{Functions}.
}}

\item{merge_optional_args}{\verb{[NULL, list]} (default \code{NULL})

Each element passed to \verb{[lexis_merge]}.
E.g. \code{list(merge_dt_harmonisers = my_harmonisers)}.}

\item{n_at_risk_dt}{\verb{[NULL, data.table]} (default \code{NULL})
\itemize{
\item \code{NULL}: No population size data is merged into the output table.
\item \code{data.table}: Merge data from this table. See \strong{Functions} and
\strong{Examples}.
\itemize{
\item It can but does not need to contain as a stratifying column
the time scale used in argument \code{observation_time_points}. If it is
included, then it must be included containing the same exact values,
e.g. with
\code{observation_time_points = list(ts_cal = 2023.9999)} you must have in
\code{n_at_risk_dt} rows with \code{ts_cal = 2023.9999}.
\item It can but does not have to contain
the time scales used in argument \code{stratum_breaks} as \code{factor} columns
where the levels must contain the lower and upper bound of each
interval,
e.g. the levels of \code{ts_age} might be
\code{c("[0,5[", "[5,10[", ..., "[80,85[", "[85,Inf[")}.
The brackets must be included but their directions are ignored.
The simple way to form a \code{factor} with such levels is \verb{[cut]}.
The lower and upper bounds must match the breaks used in argument
\code{stratum_breaks}, so for the above example you should have
\code{stratum_breaks = list(ts_cal = c(0, 5, 10, ..., 80, 85, Inf), ...)}.
}
}}

\item{weight_dt}{\verb{[NULL, data.table]} (default \code{NULL})
\itemize{
\item \code{NULL}: No direct adjusting is performed.
\item \code{data.table}: Passed to \verb{[directadjusting::directly_adjusted_estimates]}
after some harmonisation. See \strong{Functions} and \strong{Examples}.
}}
}
\value{
\strong{popEpi::prev_lexis}

Returns a \code{data.table} with
\itemize{
\item Stratifying columns defined via \code{aggre_by} (if any),
\item Stratifying time scale columns defined via \code{stratum_breaks} (if any),
\item The observation time scale column defined via \code{observation_time_points},
\item \code{n_prev}, the number of subjects in follow-up, and
\item \code{n_prev_eff}, the above plus the "extrapolated" number in follow-up, if
this was requested.
\item \code{n_at_risk}, the merged values from \code{n_at_risk_dt} if it was supplied.
\item \code{p_prev}, \code{n_prev / n_at_risk}, the proportion of prevalent records out of
\code{n_at_risk}, if \code{n_at_risk_dt} was supplied.
\item \code{p_prev_eff}, \code{n_prev_eff / n_at_risk}, the proportion of the effective
number of prevalent records out of \code{n_at_risk}, if \code{n_at_risk_dt} was
supplied and \code{n_prev_eff} was computed.
\item For \code{p_prev} and \code{p_prev_eff} the additional columns which indicate
standard error and the lower and upper confidence bounds, identified with
the suffix \verb{_se}, \verb{_lo}, and \verb{_hi}, respectively.
}
}
\description{
Function(s) to compute prevalence statistics.
}
\section{Functions}{

\strong{popEpi::prev_lexis}

\code{popEpi::prev_lexis} can be used to compute numbers of (potentially
effective numbers of) subjects remaining in follow-up at arbitrary time
points. It performs the following steps:
\itemize{
\item For each observation time point in \code{observation_time_points[[1]]}:
\itemize{
\item Calls \verb{[lexis_split_merge_aggregate_by_stratum]} with the
\code{lexis}, \code{subset}, \code{aggre_by} supplied to
\code{popEpi::prev_lexis} and with
\code{aggre_exprs = list(n_prev = quote(.N))}, and \code{breaks = stratum_breaks}.
This produces a table stratified by
\code{aggre_by} and all time scales used in \code{stratum_breaks}.
The only value column at this point is \code{n_prev}, the number
of subjects in follow-up at the given observation time point.
\item If \code{!is.null(merge_dt)},
collect subjects in \code{lexis} who were censored before the
current observation time point. This is defined as those in
\code{lexis} who have \code{lexis[["lex.Cst"]] == lexis[["lex.Xst"]]} and
\code{lexis[[obs_ts_col_nm]] + lexis[["lex.dur"]] < obs_tp}, where
\code{obs_ts_col_nm = names(observation_time_points)} and
\code{obs_tp} is the current observation time point. These collected
subjects are the ones we need to extrapolate to the current
observation time point.
\item If there are no observations that need to be extrapolated
then we end the extrapolation step early and simply use
\code{n_prev_eff = n_prev}. Otherwise proceed as described below.
\item If \code{inherits(merge_dt, "list")}, collect arguments from
\code{merge_dt} to call \verb{[surv_lexis]}.
Arguments \code{lexis}, \code{subset}, and \code{aggre_by} are assigned
internally to the respective arguments supplied to \code{prev_lexis}
except \code{subset} additionally is used to detect only those cases
who entered follow-up (any time) before the current
observation time point. It may be worth emphasizing that \code{lexis}
really is the input argument \code{lexis} and not only those whose
follow-up we need to "extrapolate".
We also set \code{estimators = "S_ch"}.
Any arguments that are passed via
\code{merge_dt} override even the internally set arguments.
The arguments not set internally or supplied by the user via
\code{merge_dt} make use of the defaults of \verb{[surv_lexis]}.
E.g.
\code{merge_dt = list(aggre_by = NULL, breaks = list(ts_fut = futs))}
are both passed to \verb{[surv_lexis]} despite what \code{aggre_by} was
for \code{prev_lexis}.
\item If \code{inherits(merge_dt, "list")}, we also interpolate survival
estimates in the table we have just produced so that there
are always 1000 intervals starting from zero and ending where
the last survival estimate is available. This is peformed to
ensure that the survival estimates we merge will be available
at a sufficient "resolution". For instance, even if the
survival estimates are for one-year intervals due to sparsity,
we get a reasonable survival estimate for a subject who was
censored at 1.01 --- from somewhere close to 1.01 instead from
2.0.
\item Merge (for the first time) \code{merge_dt} with the collected
subjects at the original exit time of each
subject. This yields the survival probability for each subject
at exit, in math \code{S(t_e)}
(starting from zero --- delayed entry is not supported).
\item Merge \code{merge_dt} for the second time, this time at the
current prevalence observation time point such as at
\code{ts_cal = 2009.999}.
In math this is \code{S(t_p)} where \code{t_p} is the prevalence
observation time point.
\item With both \code{S(t_e)} and \code{S(t_p)} available, our "extrapolated" or
"effective" number of being in follow-up is between zero and
one for each subject and defined simply as the conditional
survival up to \code{t_p} starting from \code{t_e},
\code{S(t_p|t_e) = S(t_p) / S(t_e)}. E.g.
\code{S(t_p) / S(t_e) = 0.8 / 0.9 ~ 0.8888889}.
\item Call \verb{[lexis_split_merge_aggregate_by_stratum]} for the second
time, this time with the subjects collected for extrapolation,
and sum the number of extrapolated subjects in follow-up into a
table with the identical stratification as the one created
before.
\item Add column \code{n_prev_eff} as the sum of the number of extrapolated
subjects and the original \code{n_prev} into the first table we
created.
}
\item Collect the observation time point-specific results into one big table.
\item If supplied, merge data from \code{n_at_risk_dt} into the big table. To
accomplish this, any stratifying time scale columns defined via
\code{stratum_breaks} and found also in \code{n_at_risk_dt} (the rather usual one
being age, e.g. \code{ts_age}) is assumed to be a \code{factor} column with levels
that specify the lower and upper bound of each interval in brackets, e.g.
\verb{[0, 5[}, though the brackets are ignored. The lower and upper bounds are
made use of when merging the data from \code{n_at_risk_dt} into the big table,
as the latter contains the lower and upper bounds as separate columns.
E.g. column \code{ts_age} in \code{n_at_risk_dt} with levels
\code{c("[0,5[", "[5,10[", ..., "[80,85[", "[85,Inf[")} is turned into the two
columns \code{ts_age_start = c(0, 5, 10, ..., 80, 85)} and
\code{ts_age_stop = c(5, 10, 15, ..., 85, Inf)}. The big output table contains
the very same columns if one has supplied something like
\code{stratum_breaks = list(ts_age = c(0, 5, 10, ..., 80, 85, Inf), ...)}.
Then the merge is straightforward and the big table gains the additional
column \code{n_at_risk}.
\item If \code{weight_dt} was supplied, we harmonise it with the current big
output table. Similarly to \code{n_at_risk_dt}, it is possible to include
time scales from \code{stratum_breaks} in \code{weight_dt} as a factor with
levels which are bracketed intervals, e.g.
\code{c("[0,5[", "[5,10[", ..., "[80,85[", "[85,Inf[")}, following the
format produced by \verb{[cut]}. The lower and upper bounds are extracted
and matched with the correspoding columns in the current big output
table, e.g. with \code{ts_age_start} and \code{ts_age_stop}.
\item \verb{[directadjusting::directly_adjusted_estimates]} is called to produce
adjusted estimates of prevalence proportions if \code{weight_dt} was
supplied or non-adjusted ones of not. Both \code{p_prev} and \code{p_prev_eff}
are passed to \verb{[directadjusting::directly_adjusted_estimates]}, if
the latter was estimated.
\item Even if adjusting is performed, the output table will contain
"raw" sums of \code{n_at_risk}, \code{n_prev}, and \code{n_prev_eff} if it was
computed.
}
}

